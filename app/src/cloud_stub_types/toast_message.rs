use cuteui::AppContext;

use super::CloudObject;
// 删除：云端对象操作Toast消息已禁用
// 原实现使用 OperationSuccessType 和 ObjectOperation，这些类型已被移除

pub struct CloudObjectToastMessage;

impl CloudObjectToastMessage {
    // 删除：toast_message 方法已禁用，云端功能已移除
    // pub fn toast_message(...) -> Option<String> { ... }

    // 删除：toast_deletion_confirm_message 方法已禁用，云端功能已移除
    // pub fn toast_deletion_confirm_message(...) -> Option<String> { ... }
}
